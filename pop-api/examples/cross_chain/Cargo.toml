[package]
authors = [ "R0GUE <go@r0gue.io>" ]
edition = "2021"
name = "cross-chain"
version = "0.1.0"

[dependencies]
ink = { git = "https://github.com/use-ink/ink", branch = "xcm", default-features = false }
pop-api = { path = "../../../pop-api", default-features = false, features = [ "cross-chain" ] }

[lib]
path = "lib.rs"

[features]
default = [ "std" ]
e2e-tests = [  ]
ink-as-dependency = [  ]
std = [
	"ink/std",
	"pop-api/std",
]
